{
  "name": "bazel-diff-targets",
  "version": "0.0.0",
  "private": true,
  "description": "CI-neutral CLI for computing impacted Bazel targets between Git refs.",
  "license": "MIT",
  "scripts": {
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1"
  }
}
